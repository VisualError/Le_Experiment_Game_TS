import AbstractComponent from "shared/AbstractClasses/ComponentAbstract";

// TBH I could just turn AbstractComponent into a class, but I'm lazy.
class Component extends AbstractComponent {}

export = Component;
